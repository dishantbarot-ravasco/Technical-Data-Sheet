# TDS Automation App — Python dependencies
# Generated for Render deployment (Linux / Python 3.12+)
#
# To regenerate with exact versions from your local venv, run:
#   pip freeze > requirements.txt
# (from the tds_app/ directory with your .venv active)

# ── Django core ────────────────────────────────────────────────────────────────
Django>=5.2,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3

# ── Database ───────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9          # PostgreSQL driver
dj-database-url>=2.1          # DATABASE_URL → DATABASES['default']

# ── Environment ────────────────────────────────────────────────────────────────
python-dotenv>=1.0

# ── Auth / OAuth ───────────────────────────────────────────────────────────────
google-auth-oauthlib>=1.2
requests>=2.31
bcrypt>=4.1

# ── PDF generation ─────────────────────────────────────────────────────────────
weasyprint>=62.0              # requires system packages in packages.txt
jinja2>=3.1
pypdf>=4.0                    # merges per-belt PDFs for batch "Print All" (batch_views.print_all_batch)

# ── Error monitoring ───────────────────────────────────────────────────────────
# Inert until SENTRY_DSN is set in the environment — see config/settings.py.
# Pinned below the latest release per this project's dependency-cooldown policy
# (renovate.json: minimumReleaseAge 7 days) rather than floating to newest.
sentry-sdk>=2.60.0,<3.0

# ── Production server ─────────────────────────────────────────────────────────
gunicorn>=22.0

# ── Static file serving ────────────────────────────────────────────────────────
whitenoise>=6.7               # serves frontend/ and Django admin static files

# ── Data / Excel (if used) ────────────────────────────────────────────────────
pandas>=2.2
openpyxl>=3.1

# ── Image processing ───────────────────────────────────────────────────────────
# Used by apps/services/signature_service.py to normalize a user's uploaded
# signature image (crop/resize/reformat to PNG) before storing it.
Pillow>=11.0
